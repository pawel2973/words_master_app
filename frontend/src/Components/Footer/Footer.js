import React from 'react';
import classes from './Footer.module.css';

const Footer = (props) => (
    <footer className={classes.Footer}>
        <p>WordsMaster © 2019</p>
    </footer>
);

export default Footer;